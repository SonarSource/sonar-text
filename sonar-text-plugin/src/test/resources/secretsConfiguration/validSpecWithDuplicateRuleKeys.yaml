provider:
  metadata:
    name: provider name
    category: Cloud Provider
  rules:
    - id: exampleId-1
      rspecKey: exampleKey
      metadata:
        name: rule name
        charset: "[0-9a-z\\/+]"
      detection:
        matching:
          pattern: "\\b(test pattern)\\b"
      examples:
        - text: >
            example
            text
          containsSecret: true
    - id: exampleId-2
      rspecKey: exampleKey
      metadata:
        name: rule name
        charset: "[0-9a-z\\/+]"
      detection:
        matching:
          pattern: "\\b(test pattern)\\b"
      examples:
        - text: >
            example
            text
          containsSecret: true
