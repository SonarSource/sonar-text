provider:  
  metadata:
      name: AWS
      category: Cloud provider
  rules:
    - id: exampleId
      rspecKey: aws-access-key
      metadata:
        name: AWS access key
      detection:
        matching:
          pattern: "\\b(test pattern)\\b"

