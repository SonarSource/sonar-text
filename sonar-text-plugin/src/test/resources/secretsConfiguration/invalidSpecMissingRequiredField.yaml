provider:  
  metadata:
      name: AWS
      category: Cloud provider
  rules:
    - id: aws-access-key
      metadata:
        name: AWS access key
      detection:
        matching:
          pattern: "\\b(test pattern)\\b"

